# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'RMobile' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for RMobile
   pod 'FSCalendar'
   pod 'Charts'
   pod 'DropDown'
  target 'RMobileTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'RMobileUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
